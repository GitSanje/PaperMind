export const publicRoutes: string[] = ["/", "/api/notion/callback","/signin",
  "/signup"];

export const authRoutes: string[] = [
  
  "/pdf-view",
  "/api/notion/create-page",
  "/api/notion/disconnect",
  "/api/notion/status",

];

export const apiAuthPrefix: string = "/api/auth";

export const DEFAULT_LOGIN_REDIRECT: string = "/signin";
